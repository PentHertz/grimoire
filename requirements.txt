# Grimoire runtime deps (both small; the tool degrades gracefully without them):
#   PyYAML   - parse sources.yaml
#   markdown - render docs to HTML in the viewer (else shown as plain text)
PyYAML>=6.0
markdown>=3.5
